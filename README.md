_README for gulp template_

Run the following installs in the terminal:
* npm install
* npm install gulp
* bower install?

#Important Gulp Tasks:
* -serve: rebuilds the server with newest files
* -build: uglifies the code for the browser
